import React from 'react';
import { View, Text } from 'react-native';

/**
 * HomeScreen — Landing screen after login.
 * Shows featured rooms and quick booking access.
 * TODO: Fetch rooms from useRooms() hook
 * TODO: Display user greeting with name from useAuth()
 */
export default function HomeScreen() {
  // TODO: const { rooms, loading } = useRooms();
  // TODO: const { user } = useAuth();

  return (
    <View>
      {/* TODO: Render welcome banner */}
      {/* TODO: Render featured rooms list */}
      {/* TODO: Render quick booking CTA */}
      <Text>HomeScreen — Stub</Text>
    </View>
  );
}
