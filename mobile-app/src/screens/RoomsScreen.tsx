import React from 'react';
import { View, Text } from 'react-native';

/**
 * RoomsScreen — Browse available rooms.
 * TODO: Fetch rooms via useRooms() hook with filters
 * TODO: Render paginated RoomCard list
 * TODO: Support filter by type and availability
 */
export default function RoomsScreen() {
  // TODO: const { rooms, loading, error, fetchRooms } = useRooms();

  return (
    <View>
      {/* TODO: Filter bar (type, availability) */}
      {/* TODO: FlatList of RoomCard components */}
      {/* TODO: Loading skeleton */}
      {/* TODO: Empty state */}
      <Text>RoomsScreen — Stub</Text>
    </View>
  );
}
