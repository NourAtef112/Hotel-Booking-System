import React from 'react';

/**
 * AppNavigator — Root navigation stack.
 * Switches between AuthNavigator and main tab/stack based on auth state.
 * TODO: Install @react-navigation/native and @react-navigation/stack
 * TODO: Implement auth state check via useAuth()
 */
export default function AppNavigator() {
  // TODO: const { isAuthenticated } = useAuth();

  return (
    // TODO: Return NavigationContainer wrapping:
    //   - AuthNavigator  (if not authenticated)
    //   - MainStack      (if authenticated)
    null
  );
}
